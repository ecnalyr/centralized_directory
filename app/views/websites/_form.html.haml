= simple_form_for(@website) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :domain_name
    = f.input :style
    = f.input :logo, as: :file

  .form-actions
    = f.button :submit
